apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service-deployment
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
    spec:
      containers:
      - name: gateway-service
        image: youruser/gateway-service:latest  # ToChange
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: app-configmap
        env:
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: http://discovery-service:8761/eureka/
